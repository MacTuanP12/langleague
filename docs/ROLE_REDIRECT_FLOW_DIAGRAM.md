# Role-Based Redirect Flow Diagram

## 🎯 Login Flow with Role-Based Redirect

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER LOGIN PROCESS                           │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│ Login Page   │
│ (auth-slider)│
└──────┬───────┘
       │
       │ User enters credentials
       ▼
┌──────────────────────┐
│ handleLoginSubmit()  │
│ - Clear old tokens   │
│ - Call authenticate()│
└──────┬───────────────┘
       │
       │ API Call
       ▼
┌──────────────────────┐
│ Backend API          │
│ /api/authenticate    │
└──────┬───────────────┘
       │
       │ Returns JWT Token
       ▼
┌──────────────────────────┐
│ Decode JWT Token         │
│ - Split token parts      │
│ - Base64 decode payload  │
│ - Extract authorities    │
└──────┬───────────────────┘
       │
       │ authorities = "ROLE_ADMIN,ROLE_USER"
       ▼
┌──────────────────────────┐
│ redirectUserByRole()     │
│ - Call utility function  │
└──────┬───────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────┐
│ getRouteByAuthorities(authorities)                       │
│                                                          │
│  Loop through ROLE_PRIORITY:                            │
│  ┌─────────────────────────────────┐                    │
│  │ 1. Check ROLE_ADMIN    ────────┼─── Found! → /admin │
│  │ 2. Check ROLE_STAFF    ────────┼─── Skip            │
│  │ 3. Check ROLE_USER     ────────┼─── Skip            │
│  │ 4. No match           ────────┼─── / (home)         │
│  └─────────────────────────────────┘                    │
└──────┬──────────────────────────────────────────────────┘
       │
       │ Returns: "/admin"
       ▼
┌──────────────────────┐
│ navigate(route)      │
│ React Router         │
└──────┬───────────────┘
       │
       ▼
╔═══════════════════════════════════════╗
║         REDIRECT TO TARGET PAGE        ║
╠═══════════════════════════════════════╣
║  ROLE_ADMIN  →  /admin                ║
║  ROLE_STAFF  →  /staff                ║
║  ROLE_USER   →  /dashboard            ║
║  Unknown     →  /                     ║
╚═══════════════════════════════════════╝
       │
       ▼
┌──────────────────────────────────────┐
│ PrivateRoute checks authorization    │
│ - Verify token                       │
│ - Check role permissions             │
└──────┬───────────────────────────────┘
       │
       ▼
╔═══════════════════════════════════════╗
║        RENDER TARGET DASHBOARD         ║
╚═══════════════════════════════════════╝
```

## 🔄 Priority System

```
┌─────────────────────────────────────────────────────────┐
│         When user has multiple roles:                    │
│                                                          │
│  User roles: "ROLE_ADMIN,ROLE_STAFF,ROLE_USER"         │
│                                                          │
│  Priority Order:                                        │
│  ┏━━━━━━━━━━━━━━━━━┓                                    │
│  ┃ 1. ROLE_ADMIN   ┃ ← Highest Priority (Selected!)    │
│  ┗━━━━━━━━━━━━━━━━━┛                                    │
│  ┌─────────────────┐                                    │
│  │ 2. ROLE_STAFF   │ ← Skipped                         │
│  └─────────────────┘                                    │
│  ┌─────────────────┐                                    │
│  │ 3. ROLE_USER    │ ← Skipped                         │
│  └─────────────────┘                                    │
│                                                          │
│  Result: Navigate to /admin                             │
└─────────────────────────────────────────────────────────┘
```

## 🎭 Role to Route Mapping

```
╔════════════════╦══════════════╦═══════════════════════════╗
║     ROLE       ║    ROUTE     ║      DESCRIPTION          ║
╠════════════════╬══════════════╬═══════════════════════════╣
║ ROLE_ADMIN     ║ /admin       ║ Admin Dashboard           ║
║                ║              ║ - User Management         ║
║                ║              ║ - System Settings         ║
║                ║              ║ - Full Access             ║
╠════════════════╬══════════════╬═══════════════════════════╣
║ ROLE_STAFF     ║ /staff       ║ Staff Portal              ║
║                ║              ║ - Content Management      ║
║                ║              ║ - Book Upload             ║
║                ║              ║ - Chapter Editor          ║
╠════════════════╬══════════════╬═══════════════════════════╣
║ ROLE_USER      ║ /dashboard   ║ User Dashboard            ║
║                ║              ║ - Learning Activities     ║
║                ║              ║ - My Courses              ║
║                ║              ║ - Exercises               ║
╠════════════════╬══════════════╬═══════════════════════════╣
║ Unknown/None   ║ /            ║ Home Page                 ║
║                ║              ║ - Public Content          ║
╚════════════════╩══════════════╩═══════════════════════════╝
```

## 🔐 Security Flow

```
┌────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                          │
└────────────────────────────────────────────────────────────┘

Layer 1: Login Authentication
┌──────────────────────────────┐
│ Backend validates credentials│
│ - Username/Password check    │
│ - Captcha verification       │
│ - Generate JWT with roles    │
└──────┬───────────────────────┘
       │ JWT Token with roles
       ▼

Layer 2: Frontend Redirect (UX Only)
┌──────────────────────────────┐
│ Role-based route selection   │
│ - Decode JWT                 │
│ - Get highest priority role  │
│ - Navigate to route          │
└──────┬───────────────────────┘
       │ Navigate to /admin, /staff, etc.
       ▼

Layer 3: Route Protection
┌──────────────────────────────┐
│ PrivateRoute Component       │
│ - Check token exists         │
│ - Verify user authenticated  │
│ - Check hasAnyAuthorities    │
└──────┬───────────────────────┘
       │ Authorized
       ▼

Layer 4: Backend Authorization
┌──────────────────────────────┐
│ Every API request verified   │
│ - JWT signature check        │
│ - Role validation            │
│ - Resource access control    │
└──────────────────────────────┘

⚠️  Frontend redirect is for UX only!
    Real security happens at backend!
```

## 🛠️ Component Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    FILE STRUCTURE                            │
└─────────────────────────────────────────────────────────────┘

app/
│
├── modules/account/
│   └── auth-slider.tsx ──────────┐ Uses utility
│                                  │
├── shared/                        │
│   ├── utils/                     │
│   │   ├── role-routes.ts ◄──────┘ Core logic
│   │   ├── role-routes.examples.tsx  (Usage examples)
│   │   └── ...                    
│   │                               
│   └── auth/                       
│       └── PrivateRoute.tsx ◄──────── Uses for authorization
│
├── routes.tsx ◄────────────────────── Defines protected routes
│
└── ...

tests/
└── shared/utils/
    └── role-routes.spec.ts ◄────────── Unit tests
```

## 📊 State Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    STATE MANAGEMENT                          │
└─────────────────────────────────────────────────────────────┘

Login Form State
├── email
├── password
├── captchaAnswer
└── rememberMe

       │ Submit
       ▼

Authentication Result
├── id_token (JWT)
└── authorities (decoded from JWT)

       │ Decode & Extract
       ▼

Navigation State
├── targetRoute (from getRouteByAuthorities)
└── navigate(targetRoute)

       │ Navigate
       ▼

Application State (after redirect)
├── currentUser
├── userRoles
├── permissions
└── currentRoute
```

## 🎨 Visual Example Scenarios

### Scenario 1: Admin Login
```
┌──────────┐     ┌──────────┐     ┌─────────┐
│  Login   │────▶│ Backend  │────▶│ /admin  │
│  Page    │     │  Auth    │     │Dashboard│
└──────────┘     └──────────┘     └─────────┘
   admin@         JWT Token:        Admin
   example        {                 Interface
                   auth: "ROLE_ADMIN,
                          ROLE_USER"
                  }
```

### Scenario 2: Staff Login
```
┌──────────┐     ┌──────────┐     ┌─────────┐
│  Login   │────▶│ Backend  │────▶│ /staff  │
│  Page    │     │  Auth    │     │ Portal  │
└──────────┘     └──────────┘     └─────────┘
   staff@         JWT Token:        Staff
   example        {                 Interface
                   auth: "ROLE_STAFF,
                          ROLE_USER"
                  }
```

### Scenario 3: Regular User Login
```
┌──────────┐     ┌──────────┐     ┌───────────┐
│  Login   │────▶│ Backend  │────▶│/dashboard │
│  Page    │     │  Auth    │     │   User    │
└──────────┘     └──────────┘     └───────────┘
   user@          JWT Token:        User
   example        {                 Interface
                   auth: "ROLE_USER"
                  }
```

## 🔍 Debugging Checklist

```
✅ Check browser console for logs:
   └─ "User authorities: ..."
   └─ "Found role ..., redirecting to ..."

✅ Check Network tab:
   └─ /api/authenticate response
   └─ JWT token in response

✅ Check localStorage:
   └─ jhi-authenticationToken exists
   └─ Token is valid

✅ Check route configuration:
   └─ routes.tsx has correct paths
   └─ PrivateRoute has correct authorities

✅ Check backend:
   └─ User has correct roles in database
   └─ JWT token includes auth field
```

